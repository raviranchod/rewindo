import styled from "styled-components";

export const Header = styled.header`
  padding: 2rem 0;
`;

export const Logo = styled.a`
  display: block;
  margin: 0 auto;
  max-width: 160px;
  cursor: pointer;
`;
